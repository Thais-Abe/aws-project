bucket         = "dev-sa-east-1-aws-project-tfstate"   # nome do bucket S3 que você criou
key            = "dev/terraform.tfstate"               # caminho do arquivo de estado dentro do bucket
region         = "sa-east-1"                           # região da AWS
dynamodb_table = "terraform-locks"                     # nome da tabela do DynamoDB usada para lock
